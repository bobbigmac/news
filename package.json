{
  "name": "news-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "fetch-news": "node scripts/fetch-news.js",
    "summarise": "node scripts/summarise.js",
    "dev": "npm run fetch-news && npm run summarise && npm run build && live-server docs --port=8000 --open=/",
    "watch": "live-server docs --port=8000 --open=/ --watch=src"
  },
  "dependencies": {},
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "license": "DBAD"
}
